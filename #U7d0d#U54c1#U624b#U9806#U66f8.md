# 納品手順書（CoreServer・最新版）

対象: 埋め込み型RAGチャットボット / ホスティング: CoreServer V2（GMO）
版: 2026-07-15（setup-passwordウィザード・CHAT_ACCESS_KEY・トークン上限・ロール分離を反映）
※ 旧 `DELIVERY.md` はローカル開発/一般論。実デプロイはこの手順書を正とする。

---

## 構成（ホスティングモデル）★前提
- **販売形態＝埋め込み**。顧客は**既存サイト（自社ドメイン）を保有**しており、そこに `<script>` 1行を足すだけ。
- **本体（PHP・DB・Gemini・管理画面）はベンダー側 CoreServer でホスト**（顧客ごとにサブドメイン1つ＋DB1つ＝単一テナント）。
- **顧客の既存サーバ・DBには触れない**。顧客サイトは埋め込み先（タグの設置場所）のみ。
- iframe は「顧客ドメイン → ベンダーhost」のクロスオリジン。iframe内のAPIはベンダーhost内で同一オリジン＝CORS不要。埋め込み許可は `CHAT_FRAME_ANCESTORS` で顧客ドメインに限定する。

### サーバ配置の全体像（★どれがどこで動くか）
**顧客の管理画面もベンダー統括コンソールも、すべてベンダーのCoreServer上**で動く。**顧客の自社サーバには`<script>`1行だけ**。ただし顧客インスタンスとコンソールは**同じCoreServer内の別サブドメイン（別インスタンス）**。

```
【ベンダーの CoreServer】← 下記すべてここ
├─ 顧客Aインスタンス   /domains/acme.<host>/public_html/  ├ chat/ admin/ api/ embed.js  └ DB: acme_rag
├─ 顧客Bインスタンス   /domains/beta.<host>/public_html/  ├ chat/ admin/ api/ embed.js  └ DB: beta_rag
│      … 顧客の数だけ増える（サブドメイン1つ＋DB1つ＝単一テナント）
└─ 統括コンソール      /domains/console.<host>/public_html/  └ index.php + config.php（各顧客DBへ接続）
                        ↑ 顧客インスタンスの“外”＝専用サブドメイン（全顧客DB資格情報を持つため分離）

【顧客Aの自社サイトのサーバ】← 別会社の既存サーバ。触るのは1行だけ
└─ <script src="https://acme.<host>/embed.js"></script>
```

**3つの入口（認証もURLも別）**
| 入口 | 動くサーバ/URL | ログイン | 範囲 |
|---|---|---|---|
| ①顧客の管理画面 | ベンダーCoreServer・その顧客のサブドメイン `…/<顧客FQDN>/admin/` | **顧客が初回設定したID/PW** | その1顧客（ナレッジ管理等） |
| ②ベンダー統括コンソール | ベンダーCoreServer・**専用サブドメイン** `https://console.<host>/` | **コンソール専用ID/PW** | 全顧客横断（使用量/コスト/停止） |
| （補）ベンダーが顧客画面を見る | ①と同じURL | `.env` の `ADMIN_*` | その1顧客の詳細（トークン/コスト/停止バナー） |

- ①は**顧客の数だけ**存在（各自のDBに紐づく）。②は**1つだけ**（全顧客を集約）。
- ②は全顧客のDB資格情報を持つので、**必ず顧客インスタンスとは別サブドメインに分離**し、顧客からアクセスできない場所に置く（設置手順は §8-3）。
- ローカル検証ではこの別サブドメインを別ポートで再現（①=`:8097` / ②=`:8098`）。

## 0. 役割分担
- **ベンダー（あなた）**: サーバ設定・ファイル配置・`.env`作成・ナレッジ投入・埋め込みタグ発行・トークン上限設定。
- **お客様（導入企業）**: 初回だけ「管理者ID＋パスワード」を自分で設定（setup-passwordウィザード）。以降はブックマークURLで利用、必要なら管理画面でナレッジ管理。
- **エンドユーザー**: 顧客サイトに埋め込まれたウィジェットだけを使う（管理画面・標準チャットには入れない）。

---

## 1. サーバ準備（サブドメインとdocroot）★ハマりどころ
1. CoreServerパネル「ウェブ → サブドメイン管理」で対象サブドメイン（例 `rag.example…`）を作成。
   - 作成時のドキュメントルートは **既定＝`/domains/<サブドメインFQDN>/public_html`** でよい。
2. **既存サブドメインの場合は docroot を必ず確認**。過去のフォルダ（例 `RAG_coreserver_deploy/app/…`）を指していると、正しく配置しても **404** になる。
   - CoreServerのサブドメインは **docrootを後から編集できない**。ズレていたら「**削除→同名で再作成**（デフォルトdocrootに戻る）」。削除時「ディレクトリ内容を削除」は**チェックしない**（ファイルを残す）。
3. SSL（Let's Encrypt）でHTTPS化。

---

## 2. ファイル配置
配布ZIP（`coreserver_ready`）の**中身をドメイン直下 `/domains/<FQDN>/` にそのまま展開**する。展開後：
```
/domains/<FQDN>/
├─ public_html/     ← 公開領域（chat/ admin/ api/ assets/ embed.js bootstrap.php setup-password.php …）
├─ .env             ← 非公開（public_htmlの外）
├─ vendor/ src/ vault/ storage/ sql/ scripts/
└─ *.md
```
- **`public/` の中身が `public_html/` に、それ以外は public_html の“外”**。リネーム・移動は不要（ZIPが既にこの構造）。
- `storage/` に書き込み権限。

---

## 3. データベース（作成は手動・テーブルは自動）
1. CoreServer「データベース → MySQL(MariaDB)」で**新規作成** → **DB名・ユーザー名・パスワード・ホスト(localhost)** を控える（一度きり表示）。※共用ホストのため「DBそのものの作成」は手動（自動化不可）。
2. **テーブル作成（schema.sql）は不要**。`.env` のDB情報が正しければ、**初回アクセス時にアプリが自動でテーブルを作成**する（`CREATE TABLE IF NOT EXISTS`＋初期化ロックで安全）。
   - うまく作成されない場合の保険として、phpMyAdmin で `sql/schema.sql` を手動インポートしてもよい。
   - 初期シードは LLM/Embedding を空にしてあり、**プロバイダ設定は `.env`（Gemini等）に委ねる**。表示名などは**汎用の中立既定**（ボット名＝「AIアシスタント」等）で入るので、§6.5 で顧客ごとに変更する。

---

## 4. `.env` 作成（ベンダーが記入・public_htmlの外）
`.env.example` を基に作成。最低限：
```
APP_URL=https://<FQDN>
DB_HOST=localhost
DB_NAME=<作成したDB名>
DB_USER=<作成したユーザー>
DB_PASSWORD=<そのパスワード>

LLM_PROVIDER=gemini
GEMINI_API_KEY=<Gemini APIキー（AQ.Ab8… 形式）>     # ベンダー発行（1本目）
GEMINI_API_KEY_2=<2本目・任意>                      # 複数入れると自動フェイルオーバー
GEMINI_API_KEY_3=<3本目・任意>
GEMINI_MODEL=gemini-flash-latest                    # ★現行版に追従するエイリアス（提供終了で切れない）
EMBEDDING_PROVIDER=gemini
GEMINI_EMBEDDING_MODEL=gemini-embedding-001         # ★旧 text-embedding-004 は404。これが現行

APP_ENCRYPTION_KEY=base64:<32バイト乱数>   # php -r "echo 'base64:',base64_encode(random_bytes(32));"

# チャット標準ページのアクセス鍵（顧客のブックマークURL用）★必須（未設定だとsetup画面も開けない=安全側）
CHAT_ACCESS_KEY=<長い乱数（例 40桁hex）>

# 埋め込みを許可する顧客サイトのオリジン（クロスオリジン埋め込みの許可先）★埋め込み販売では実質必須
# 空だとどのサイトからでも埋め込める。顧客サイトのドメインに限定する。
CHAT_FRAME_ANCESTORS="https://顧客ドメイン https://www.顧客ドメイン"

# 保守プラン別トークン上限（0/空=無制限）
MONTHLY_TOKEN_LIMIT=1800000        # ¥3,000=1800000 / ¥5,000=4000000 / ¥10,000=10000000
MONTHLY_TOKEN_COST_PER_1M=250      # ベンダー画面の推定コスト表示用

# サービス稼働フラグ（キルスイッチ）: 1=稼働 / 0=停止（未払い等・可逆）。§8-2
SERVICE_ACTIVE=1
SERVICE_SUSPENDED_MESSAGE=         # 停止中の表示文（空=安全な既定文）。第三者が見る前提で中立に

# 管理ログイン（この /admin/ は顧客専用。ベンダーはログインしない）
# 顧客＝CUSTOMER_*：ナレッジ/表示設定/フィードバックのみ（トークン・コスト・LLM設定は表示されない）。
# ★空のままにする。お客様が setup-password ウィザードで自分で設定する（未設定の間は誰もログイン不可＝安全側）。
CUSTOMER_USERNAME=
CUSTOMER_PASSWORD_HASH=
```
- **CUSTOMER_* は空のまま**にし、お客様が初回ウィザードで設定（＝顧客ロール）。
- **ベンダーロール(ADMIN_*)は廃止**。ベンダーは顧客の`/admin/`に入らず、**統括コンソール**で全顧客を管理する。
- **LLM/プロバイダ/モデル/キーは `.env`（上記 `LLM_PROVIDER`・`GEMINI_*` 等）で設定**。管理画面のLLM設定UI（旧 settings.php）は撤去済み。
- `CHAT_ACCESS_KEY` は**必ず設定**（未設定だと初回ウィザードもチャット標準ページも fail-closed で開けない）。
- **モデル名は固定版を書かない**：`gemini-flash-latest` / `gemini-embedding-001` を使う。固定バージョン（例 `gemini-2.5-flash`・`text-embedding-004`）は提供終了で**ある日突然404**になり、回答生成・ベクトル検索が止まる（過去に発生）。
- **プロバイダ/モデルは自由に切替可（顧客ごとに別でもOK）**：`LLM_PROVIDER` を `gemini`/`openai`/`anthropic`/`groq`/`ollama` から選び、そのプロバイダのキー＋`*_MODEL` を書くだけ。**モデル名はホワイトリストなしの自由指定**（GPT-5系・Claude Opus/Sonnet/Haiku・ローカルの任意モデル等、文字列をそのままAPIへ渡す）。単一テナントなので**A社=Gemini / B社=Claude / 機密のC社=ローカルLLM**のような顧客別の出し分けが `.env` or 管理画面設定でできる。※**埋め込み**プロバイダを変えた顧客だけ**ナレッジ再ベクトル化が必要**（LLMだけの変更は再indexなし）。
- **APIキー複数対応（フェイルオーバー）**：`GEMINI_API_KEY` / `_2` / `_3` に複数入れると、**1→2→3→1… の順で自動切替**。あるキーが上限(429)・権限エラー・無効キー・一時障害で「だめ」なら次のキーへ回し、**全キー一周して全滅したときだけ**エラーを返す。成功キーの次から始めるので負荷は分散。1本でも従来どおり動作。
- **★データ学習の扱い（本番は有料枠必須）**：Geminiは**有料枠（課金有効）なら入出力を学習に使わない**が、**無料枠は学習＋人間レビューの対象**（Google規約）。顧客に「学習されない」と約束するなら、**課金を有効にした有料キー**を使うこと（無料枠の複数キー分散は不可）。機密資料の顧客はローカルLLM（Ollama等）への切替も選択肢。導入時に**課金が有効か必ず確認**。

---

## 5. 初回アカウント設定（お客様に一度だけ実施してもらう）
1. お客様に **`https://<FQDN>/setup-password.php?k=<CHAT_ACCESS_KEY>`** を案内。
2. お客様が **ユーザー名（ID）＋パスワード** を入力（ID:3〜64字・英数記号 . _ - @／PW:10文字以上・大小英字/数字/記号・よくある語不可）。
   - 送信で `.env` の **`CUSTOMER_USERNAME` と `CUSTOMER_PASSWORD_HASH`（＝顧客ロール）** を自動書込み。これで顧客がログイン可能になる（ベンダーの`ADMIN_*`は不要）。
   - これにより、お客様は**トークン数・コスト・LLM設定が見えない顧客ロール**でログインする。
3. **控えを保存**（ダウンロードボタン or 記録）。控え.txtには **ブックマークURL・管理URL・ID・パスワード** が入る。パスワードは再表示不可。
4. 管理画面ログイン確認後、**「この設定画面を削除」**（確認3段階）でウィザードを自己削除。
   - 既に顧客ハッシュ設定済みなら、この画面からは変更不可（乗っ取り防止。再設定はベンダーが`.env`直編集）。

---

## 6. ナレッジ投入
### ② フォルダ同期（メイン）
1. `public_html/sync-run.php` の13行目トークンを設定（`CHAT_ACCESS_KEY`とは別の強い値）。
2. `vault/` に `.md` を配置（`README.md`・`_`始まりは対象外）。
3. `https://<FQDN>/sync-run.php?token=<設定値>` を実行 → `OK added=… ` を確認。
4. CoreServer CRON で定期自動同期（例: 30分毎 `curl -s "https://<FQDN>/sync-run.php?token=…"`）。
### ① 管理画面アップロード（PDF/Word/Excel）
管理画面 → ナレッジ → アップロード（自動でチャンク化＋ベクトル化）。②と併用可。

---

## 6.5 表示設定の顧客別カスタマイズ（★導入ごとに必ず実施）
初期状態は**汎用の中立既定**（ボット名＝「AIアシスタント」等）。**顧客の業種・用途に合わせて必ず変更**する。管理画面 → **「🎨 表示設定」**（`admin/appearance.php`・顧客も編集可）で以下をフォーム編集：
- **ボット名**（`product_name`）：例「◯◯サポートAI」。※未変更だと全顧客共通の汎用名のまま表示される。
- **あいさつ文**（`welcome_message`）：チャットを開いた最初の吹き出し。
- **候補質問**（`opening_quick_replies`）：あいさつ下のワンタップ質問（初期は空）。顧客のよくある質問を3〜4個。
- **回答範囲の説明**（`topic_description`）：ボットが何に答えるかの内部説明（回答方針に影響）。
- **アクセント色/位置**：埋め込みタグ側の `data-color` / `data-title` / `data-position`（§7）。
> HR（社内規程）デモの表示に一括で戻したい場合のみ `sql/_seed_hr_domain.sql` を実行（通常は使わない）。

---

## 7. 埋め込み設置（エンドユーザー向け）
顧客サイトの任意ページに1行：
```html
<script src="https://<FQDN>/embed.js"
        data-title="サポートAI" data-color="#2563eb"></script>
```
- 埋め込みは `?embed=1` で読み込まれ、管理リンクは出ない・アクセス鍵不要（公開ウィジェット）。

### 7.5 埋め込み時の事前チェック（顧客サイト側の環境依存を回避）
顧客の既存サイトに載るため、以下を事前確認すると不具合を防げる。なおウィジェットは顧客サイトの要素を書き換えないので**サイト機能を壊すことはない**（最悪でも「表示されない/隅で被る」）。
- **HTTPS**：顧客サイトがHTTPSであること（HTTPだと混在コンテンツで弾かれる）。
- **CSP（最重要）**：顧客サイトが Content-Security-Policy を使っている場合、ベンダーhostを許可してもらう。未設定なら不要。ウィジェットが出ない時はまずCSPを疑う。
  ```
  script-src ... https://<ベンダーhost>;
  frame-src  ... https://<ベンダーhost>;
  style-src  ... 'unsafe-inline';
  ```
- **右下の被り**：顧客サイトが同じ隅に別UI（他チャット/トップへ戻る/Cookie同意バナー）を持つ場合、`data-position="left"` で反対側へ。
- **依存排除の状況（対策済）**：チャット/管理UIとも**外部CDN不使用**（Tailwind/Alpineはローカル同梱）。embed.jsは**ES5相当で広い互換性**・外部依存なし。会話継続は**Cookie非依存**（session_uuid/sessionStorage）。訪問者Cookieは `SameSite=None; Secure` で第三者環境でも可能な範囲で機能（使えなくてもチャットは動作）。

---

## 8. プラン別トークン上限＆サービス停止（ベンダー管理）
### 8-1. トークン上限・使用量・コスト
`.env` の `MONTHLY_TOKEN_LIMIT` を保守プランに合わせて設定。詳細表は `保守プラン_トークン上限表_内部用.md`。
- **これが実際のブロック**：当月の消費トークンが上限に達すると、`Generator` が**LLMを呼ばず打ち切り**（それ以上の課金を止める）→顧客・お客様には数字/費用を出さない汎用文。毎月1日リセット。80%＝警告ライン。
- ★**統括コンソールの `token_limit`（config.php）は“％表示専用”**。実ブロックは各顧客の `.env MONTHLY_TOKEN_LIMIT`。**両者を同じ値**にする（ズレるとコンソールの%が実態と食い違う）。
- 「トークン上限（毎月リセット）」と「停止スイッチ`service_active`（未払い等・手動・リセットされない）」は別物。
- 使用量/推定コストは、その顧客の管理ダッシュボード（ベンダーロール）、または**全顧客を横断する「ベンダー統括コンソール」**で確認（§8-3）。

### 8-2. サービス停止スイッチ（未払い等の接続断）
停止中はLLMを呼ばず**中立メッセージのみ**返す（DB・ナレッジは保持＝可逆）。判定は2系統：
- **(a) DBフラグ `service_active`**（既定`1`）＝通常の停止/再開。**統括コンソールの「停止/再開」ボタン**（§8-3）で切替。顧客ロールからは変更不可。
- **(b) `.env SERVICE_ACTIVE=0`**＝強制停止の上書き（DBより優先。ファイル編集が要る“ハードロック”）。
- 停止文は `.env SERVICE_SUSPENDED_MESSAGE`（空なら安全な既定文）。**顧客サイトの訪問者=第三者が見る前提**なので「未払い」等は書かない。
- 顧客の管理ダッシュボード上部にも状態バナー（緑=稼働／赤=停止・ベンダーのみ表示）。
- 運用注意：契約に「支払い遅延◯日で停止」を明記＋停止前に通知。黙って切らない。解約確定までデータは消さない。

### 8-3. ベンダー統括コンソール（全顧客を1画面で管理・ベンダー専用ツール）
**顧客には配布しない別アプリ**（全顧客のDB資格情報を持つ）。管理用サブドメイン等に設置し `config.php`（顧客レジストリ＋ログイン）を用意する（詳細は内部の `導入実行ガイド_AI指示書.md`）。
- **サマリーカード**（顧客数/稼働中/停止中/今月トークン合計/推定コスト合計）＋**顧客別トークンの棒グラフ**（上限%で色分け）＋**明細表**。
- 明細表の行ごとに**停止/再開ボタン**（対象顧客DBの `service_active` を書換）。
- ログイン（ID/PW・5回失敗ロック）＋CSRF＋接続エラー行の耐性あり。グラフ/表とも**外部CDN不使用**（インラインCSS）。`config.php` は直アクセス禁止（同梱`.htaccess`）で保護。

---

## 9. 動作確認チェックリスト
- [ ] `https://<FQDN>/chat/`（鍵なし）→ **404**（部外者が入れない）
- [ ] ブックマークURL `…/chat/?k=<鍵>` → チャット表示
- [ ] `…/setup-password.php?k=<鍵>` → ID/PW設定 → 控えDL → 画面削除
- [ ] `/admin/` → 設定したID/PWでログイン可（5回失敗で10分ロック）
- [ ] チャットで質問 → ナレッジから出典付き回答
- [ ] 埋め込みタグを顧客サイト（またはテストページ）に貼って表示
- [ ] **Gemini APIキーが課金有効（有料枠）**＝入出力が学習に使われない（顧客への約束の前提）
- [ ] `SERVICE_ACTIVE=1`（稼働）になっている

---

## 10. 顧客に渡すもの / 見せないもの
- 渡す: ブックマークURL、管理ID/PW（お客様自身が設定）、埋め込みタグ、月あたり目安回数。
- 見せない: トークン数・上限値・原価・利益率・モデル名・API単価・`.env`。

---

## 11. トラブルシュート
| 症状 | 原因 | 対処 |
|---|---|---|
| `/chat/` や `/embed.js` が404 | サブドメインのdocrootが別フォルダを指す | §1-2（削除→再作成でdocroot正常化） |
| 「セットアップが必要です／root接続拒否」 | `.env`のDB情報未設定（rootで接続） | §4のDB4項目を実値に |
| 何を聞いても「見当たりません」 | ナレッジ未投入 | §6の同期を実行 |
| setup-password.php が404 | `CHAT_ACCESS_KEY`未設定 or `?k=`不一致 | §4で鍵設定＋正しいURL |
| ログインできない | `ADMIN_PASSWORD_HASH`未設定 | お客様が§5を実施（またはベンダーが`.env`直編集） |

---

## 12. よくあるエラーとQ&A（実地検証で判明したもの）
> 過去に実際に踏んだ不具合と対処。**同じ症状が出たらまずここを見る**。原因の多くは「モデル名の固定バージョン」と「スキーマ/設定の初期値」。

### Q1. チャットで「システムエラーが発生しました」。検索結果（出典）は出るのに回答文だけ出ない。
**原因**：LLMモデル名が古く、Gemini APIが `404`（例：`gemini-2.5-flash` →「no longer available to new users」）。**回答生成だけが失敗**する（検索は動くので出典は出る）。
**対処**：`.env` の `GEMINI_MODEL=gemini-flash-latest`（現行版に追従するエイリアス）にする。固定版は書かない。
**確認**：`curl -s -X POST "https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent" -H "x-goog-api-key: <KEY>" -H "Content-Type: application/json" -d '{"contents":[{"parts":[{"text":"hi"}]}]}'` が本文を返せばOK。

### Q2. 回答は出るが、言い換え質問に弱い／精度が低い。ベクトル検索が効いていない気がする。
**原因**：埋め込みモデル名が古く `404`（例：`text-embedding-004` →「not found for embedContent」）。埋め込み生成が失敗し、**キーワード検索だけに縮退**（無言でフォールバックするので気づきにくい）。
**対処**：`.env` の `GEMINI_EMBEDDING_MODEL=gemini-embedding-001` にして、**ナレッジを再同期**（§6）。
**確認**：`SELECT COUNT(*) FROM chunks WHERE embedding IS NOT NULL;` が 0 なら埋め込み未生成。1件以上あればベクトル化済み。

### Q3. フォルダ同期(`sync-run.php`)が `Column not found: 'source_ref'` で失敗する。
**原因**：`documents` テーブルにナレッジ源の追跡列（`source_type/source_ref/source_hash`）が無い。古いスキーマで作成されたDB。
**対処**：現行 `sql/schema.sql` は同列を含む（自動スキーマでも作られる）。既存DBには `sql/migration_source.sql` を phpMyAdmin で適用。

### Q4. ボット名が「AIアシスタント」（または「社内規程アシスタント」）のまま。顧客名になっていない。
**原因**：表示設定を導入時に変更していない（初期値のまま）。
**対処**：**§6.5** の通り、管理画面→設定で `ボット名/あいさつ/候補質問/回答範囲` を顧客向けに変更。埋め込みタグの `data-title/data-color` も合わせる。

### Q5. 「一時的にご利用いただけません」等が出て回答しなくなった。
まず**どの停止か**を切り分ける（管理ダッシュボード上部のバナーで一目でわかる）：
**原因A（停止スイッチ）**：`SERVICE_ACTIVE=0`（未払い等でベンダーが停止）。→ 入金確認後 `=1` に戻せば即復旧（§8-2）。ダッシュボードに赤バナー。
**原因B（トークン上限・正常）**：保守プランの月間トークン上限に到達（`MONTHLY_TOKEN_LIMIT`）。毎月1日リセット。使用量はベンダー用ダッシュボードで確認。
**原因C（キー枯渇）**：Gemini APIキーが全て上限(429)に到達。複数キー（`GEMINI_API_KEY_2/_3`）を入れると1→2→3→1…で自動切替し、**全キー一周して全滅したときだけ**このエラー。
**対処**：A=フラグ戻す／B=プラン見直し／C=キー追加・枠リセット待ち。

### Q6. APIキーを1本差し替えたら動かない／「All Gemini API keys are exhausted」。
**原因**：貼り付けたキーが無効（`400 API_KEY_INVALID`）。無効キーは自動で次のキーへ回すが、**全キーが無効/枯渇**だとこのエラー。
**対処**：キーの値を再確認（前後の空白・改行混入に注意）。1本でも有効なら復帰する。※`400`でも「APIキー無効」は自動フェイルオーバー対象。ただし `404`（モデル無し）や不正リクエストは全キー共通なので即エラー（真因を隠さないため）。

### Q7. `/chat/` や `/embed.js` が 404。ファイルは置いたのに。
**原因**：サブドメインのドキュメントルートが旧フォルダを指している（CoreServerはdocroot後編集不可）。
**対処**：§1-2（サブドメインを削除→同名で再作成し、docrootを既定の `public_html` に戻す。「ディレクトリ内容を削除」はチェックしない）。

### Q8. 埋め込みタグを貼ったのにウィジェットが出ない。
**原因**：顧客サイトの Content-Security-Policy がベンダーhostを許可していない／HTTPSでない／同じ隅に別UIが被っている。
**対処**：§7.5（CSPに `script-src`/`frame-src` でベンダーhostを追加、HTTPS化、`data-position="left"` で回避）。

### Q9. 環境やPHP/ライブラリのバージョン差で動かないのが怖い。
**対処（対策済み）**：チャット/管理UIとも**外部CDN不使用**（Tailwind/Alpine等はローカル同梱）。embed.jsは**ES5相当・外部依存なし**。会話継続は**Cookie非依存**（session_uuid）。DBテーブルは初回アクセスで自動作成。→ 環境依存は基本的に排除済み。残る外部依存は「Gemini APIのモデル名」だけなので、**latestエイリアス運用（Q1/Q2）**を守ること。
