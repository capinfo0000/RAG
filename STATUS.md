# STATUS — 現状まとめ（共有安全）

> このファイルは共有安全（秘密なし）。実値・認証情報・料金等の機微は含めない。
> 詳細な引き継ぎは別途 share-safe な記録（PROJECT_LOG）で管理。

最終更新: 2026-07-23

## このリポジトリの位置づけ
- 埋め込み型RAGチャットボット製品まわりの作業リポジトリ。
- 製品本体（PHP/DB/Gemini/管理画面）は別途ローカル開発・CoreServer本番にデプロイ済みで稼働中。
- 本リポジトリでは主に「セッション運用の基盤（自動ログ・Web動作確認ツール）」を管理する。

## 本セッションで追加したもの
### 1. 会話ログ自動記録フック（`.claude/`）
- `.claude/hooks/chatlog-record.js` … Stopフックで、AIが回答するたびに会話を記録。
  - 出力: `logs/<project>/<date>_<session8>_chat.md`（**gitignore**。コンテナ内保管・公開しない）
  - **User: / Assistant:** で区別、tool出力・サブエージェントは除外
  - 秘密（APIキー/トークン/パスワード/DB認証/アクセス鍵）は自動で `[REDACTED]` 化
- `.claude/settings.json` … 上記を起動する Stop フック設定
- 全クラウドプロジェクトへ横展開する場合は `tools/setup-chatlog.sh` を環境のセットアップスクリプトに登録（冪等）。

### 2. Web動作確認ツール（`tools/webcheck.js`）
- 公開URLの HTTP ステータス / タイトル / 埋め込みタグ / アセット配信を確認するヘルパー。
- クラウドのヘッドレスブラウザ描画は egress プロキシ制約で不可のため、HTTP/内容レベルの確認に用いる。

## 直近の動作確認（本番サイト・HTTP/内容レベル）
- ルート `/` … 200 / タイトル「埋め込み型RAGチャットボット｜デモ」
- `embed.js` 配信 200、ローカル同梱アセット配信 200（CDN撤廃が反映済み）
- 埋め込みウィジェット＋自動オープンのマークアップを確認

## 運用メモ
- クラウドでできる: 公開ページの HTTP/内容確認（curl / WebFetch / `webcheck`）。
- クラウドでできない: ヘッドレスブラウザの描画・操作（プロキシ制約）、ログインが要る管理画面操作（ローカルの実ブラウザ専用）。
- 秘密は本リポジトリに一切コミットしない（`.gitignore` で `.env*` / `logs/` 等を除外）。

## 次の作業候補
- 露出済み認証情報のローテーション（検証一段落後）。
- 製品コードのソース一本化・push 状況の確認。
- `webcheck` の環境セットアップ登録（任意）。
